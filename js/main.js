import { initSearchBar } from './components/search.js';
import { initHeaderBarHide } from './components/header_bar.js';

document.addEventListener('DOMContentLoaded', () => {
	initSearchBar();
	initHeaderBarHide();
});
